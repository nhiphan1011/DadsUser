export namespace NASSISTANT_MODEL {
    export interface IActSkill {
        objective: Array<string>
        act: string;
        skill: string;
        topic: string;
        question: string;

    }
}
